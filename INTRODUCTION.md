# メール自動化システム - note.com 紹介記事

## はじめに

毎日大量に届くメールの処理に時間を取られていませんか？このツールは、Outlookメールの処理を自動化し、あなたの時間を取り戻します。

## このツールでできること

### 📧 メールの自動分類と処理

受信したメールを自動的に分類し、適切に処理します：

- **重要なメール（To: 自分宛）**
  - AI要約
  - 返信下書き自動生成
  - Obsidianに保存
  - 未読のまま維持

- **参考メール（CC: 自分宛、グループ宛+名前あり）**
  - AI要約のみ
  - Obsidianに保存
  - 未読のまま維持

- **不要なメール**
  - セールスメール → 自動で既読
  - Teams通知 → 未読のまま（AI処理なし）

### 🤖 AIによる自動処理

- **要約生成**: 長いメールも一目で内容を把握
- **返信文生成**: 過去のメールから文体を学習し、自然な返信を生成
- **TODO抽出**: メール内のタスクを自動抽出
- **異常検知**: 定期メールの異常を自動検知してSlack通知

### 📝 Obsidian連携

- メール内容を自動でObsidianに保存
- 日付別、送信者別のインデックス自動生成
- タグとリンクで簡単に検索
- プロジェクトキーワードで自動分類

## 導入メリット

### ⏰ 時間の節約

- メールチェックの時間を50%削減
- 重要なメールを見逃さない
- 返信文作成の時間を削減

### 🎯 集中力の維持

- 不要なメールに気を取られない
- 重要なメールだけに集中できる
- 通知に邪魔されない

### 📊 メールの可視化

- Obsidianでメール履歴を一元管理
- プロジェクト別、送信者別に整理
- グラフビューで関係性を可視化

## 技術スタック

- **Python 3.11+**
- **OpenAI GPT-4**: メール要約と返信生成
- **Outlook COM / Graph API / IMAP**: メール取得（環境に応じて選択可能）
- **Obsidian**: ナレッジベース構築
- **Slack**: 異常通知

**GitHubリポジトリ**: https://github.com/legacy2aiengineer-droid/mail-automation

## 対応環境

### メール接続方法（環境に応じて選択）

1. **Outlook COM**（Windows専用、推奨）
   - 基本認証不要
   - Outlookデスクトップアプリを使用
   - IT管理者の承認不要

2. **Microsoft Graph API**（推奨、クロスプラットフォーム）
   - OAuth2認証（安全）
   - 管理者承認が必要な場合あり

3. **IMAP/SMTP**（一般的な方法）
   - 基本認証が有効な環境で使用可能
   - アプリパスワードが必要な場合あり

4. **Exchange Web Services (EWS)**（IMAP無効でも動作）
   - MacOS、Linux、Windows対応

## セットアップ手順

### 1. 環境変数の設定

```bash
# .envファイルを作成
cp env.example .env

# 必須項目を設定
EMAIL_ADDRESS=your-email@company.com
OPENAI_API_KEY=sk-...
OBSIDIAN_VAULT_PATH=/path/to/vault
```

### 2. テンプレートのカスタマイズ

`templates/` フォルダ内のテンプレートファイルを編集：

**社内用** (`internal_template.txt`):
```
{sender_name}様

お疲れさまです、{my_name}です。

{body}

よろしくお願いいたします。
```

**社外用** (`external_template.txt`):
```
{sender_company}　{sender_name}様

お世話になっております。
{my_company}の{my_name}です。

{body}

以上、よろしくお願いいたします。
```

### 3. 実行

```bash
# 手動実行
./run.sh

# 自動実行設定（macOS）
./install_launchagent.sh
```

## 使用例

### 受信メールの自動処理

```
受信: [To: あなた@会社.com] プロジェクト会議のご連絡
  ↓
📧 パターン1: 要約+返信下書き作成
  ↓
結果:
  - 要約がObsidianに保存
  - 返信下書きが自動作成
  - メールは未読のまま
```

```
受信: [CC: あなた@会社.com] チーム連絡（本文に名前あり）
  ↓
📬 パターン2: 要約のみ
  ↓
結果:
  - 要約がObsidianに保存
  - 返信は作成されない
  - メールは未読のまま
```

```
受信: [From: newsletter@example.com] キャンペーンのお知らせ
  ↓
🗑️ 除外: 既読
  ↓
結果:
  - AI処理なし
  - 自動で既読
```

## カスタマイズ例

### 除外設定

```bash
# Teams通知は未読維持（後で確認）
EXCLUDE_SENDER_DOMAINS=@teams.mail.microsoft

# セールスメールは既読にする
EXCLUDE_AND_READ_DOMAINS=@newsletter.com,@sales.example.com
```

### グループメール対応

```bash
# グループメールアドレスを設定
GROUP_EMAIL_ADDRESSES=team@company.com,project@company.com

# 本文に名前があれば要約する
MY_NAME=山田
```

### 返信文のカスタマイズ

テンプレートファイルを編集するだけ！

```
# より親しみやすい社内用
{sender_name}さん

お疲れ様です！{my_name}です。

{body}

よろしくです！
```

## よくある質問

### Q: OpenAI APIの料金は？

A: GPT-4を使用するため、メール1通あたり約5〜10円程度です。月間100通のメールで500〜1000円程度です。

### Q: 会社のセキュリティポリシーは大丈夫？

A: Outlook COMを使用すれば、既存のOutlook設定を利用するため、IT部門の承認が不要な場合が多いです。ただし、必ず会社のポリシーを確認してください。

### Q: 誤って重要なメールを既読にしてしまったら？

A: ルールベース判定を使えば、重要なメールは必ず未読のまま維持されます。また、すべて未読維持する設定（`MARK_AS_READ_AFTER_PROCESS=1`）も可能です。

### Q: MacOSでも使える？

A: はい、IMAP/SMTP、Graph API、EWSが使用可能です。Outlook COMのみWindows専用です。

## セキュリティ考慮事項

- `.env`ファイルは`.gitignore`に含まれており、Gitにコミットされません
- 認証情報はローカルにのみ保存されます
- OpenAI APIにメール内容が送信されます（機密情報に注意）
- Obsidian vaultはローカルストレージに保存されます

## ライセンス

MIT License

## サポート

問題が発生した場合は、以下をご確認ください：

1. ログファイル: `logs/mail_automation_YYYY-MM-DD.log`
2. README.mdのトラブルシューティングセクション
3. 各メールプロセッサーの接続テスト

## まとめ

このツールを使えば：

- ✅ メール処理時間を大幅削減
- ✅ 重要なメールを見逃さない
- ✅ 自然な返信文を自動生成
- ✅ Obsidianでメール履歴を管理
- ✅ 業務効率を向上

ぜひお試しください！

---

## 更新履歴

- 2025-11: 初版リリース
  - メール自動分類機能
  - 社内外テンプレート対応
  - 除外メール機能
  - フローC改善（1日1回実行）

