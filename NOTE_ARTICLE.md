# 【AI×メール】大量のメールを自動処理！Outlookメール自動化システムを作った話

## TL;DR

- Outlookメールの処理を完全自動化するPythonツールを作成
- GPT-4でメール要約＆返信文を自動生成
- 重要なメールだけ未読で残し、不要なメールは自動既読
- 社内外で返信テンプレートを自動切替
- Obsidianでメール履歴を一元管理

**GitHubリポジトリ: （あなたのリポジトリURL）**

---

## はじめに：メール地獄からの脱出

毎日届く大量のメール、あなたはどう処理していますか？

- 朝一番でメールチェックに30分
- 重要なメールが埋もれて見逃す
- CCメールの処理に迷う
- セールスメールで受信トレイが埋まる
- 返信文を考えるのに時間がかかる

私も同じ悩みを抱えていました。そこで、**メール処理を完全自動化するツール**を作りました。

## このツールの特徴

### 🎯 1. インテリジェントなメール分類

メールを自動的に分類し、適切に処理します：

```
┌─────────────────────────────────────┐
│ 受信メール                           │
└─────────────────────────────────────┘
              ↓
    ┌─────────┴─────────┐
    │    除外判定        │
    │  （最優先）        │
    └─────────┬─────────┘
              ↓
    ┌─────────┴─────────────┐
    │ セールスメール？       │
    │ → 🗑️ 既読            │
    ├───────────────────────┤
    │ Teams通知？           │
    │ → 🚫 未読維持        │
    └─────────┬─────────────┘
              ↓
    ┌─────────┴─────────────┐
    │ To: 自分宛？          │
    │ → 📧 要約+返信生成    │
    ├───────────────────────┤
    │ CC/グループ+名前？     │
    │ → 📬 要約のみ         │
    ├───────────────────────┤
    │ その他                │
    │ → ✓ 既読             │
    └───────────────────────┘
```

### 🤖 2. GPT-4による自動処理

**メール要約**
```
【元のメール（500文字）】
プロジェクトAの進捗について...（長文）

↓ AI要約

【要約（100文字）】
・進捗80%
・次回MTGは金曜14時
・資料準備をお願いします
```

**返信文生成**
```
過去のメールから文体を学習
→ 自然な返信文を自動生成
→ 下書きに保存
```

### 📧 3. 社内外テンプレート自動切替

**社内メール（@company.com）**
```
山田様

お疲れさまです、中村（康）です。

会議の件、承知いたしました。

よろしくお願いいたします。
```

**社外メール（その他）**
```
ABC株式会社　田中様

お世話になっております。
株式会社カナデビアの中村です。

お見積もりの件、承知いたしました。

以上、よろしくお願いいたします。
```

### 📝 4. Obsidian連携

すべてのメールをObsidianに自動保存：

- 日付別インデックス
- 送信者別インデックス
- タグで自動分類
- リンクで関連付け

```
01_Index/
  Mail/
    20251108_103045_プロジェクト会議.md
    20251108_105012_見積依頼.md
  日次/
    2025-11-08.md  ← その日のメール一覧
  送信者/
    山田太郎.md    ← 送信者別一覧
```

## 実際の効果

### 導入前
- メール処理時間: 1日60分
- 見逃し: 月2〜3件
- 返信作成: 1通5分

### 導入後
- メール処理時間: 1日20分（**67%削減**）
- 見逃し: ゼロ
- 返信作成: 1通1分（AI生成を確認・修正するのみ）

## 技術的なポイント

### メール取得方法の柔軟性

環境に応じて4つの接続方法から選択可能：

| 方法 | 対応OS | 認証 | 推奨度 |
|-----|--------|------|--------|
| Outlook COM | Windows | 不要 | ⭐⭐⭐⭐⭐ |
| Graph API | 全OS | OAuth2 | ⭐⭐⭐⭐ |
| EWS | 全OS | 必要 | ⭐⭐⭐ |
| IMAP/SMTP | 全OS | 必要 | ⭐⭐ |

### ルールベース判定の実装

```python
def get_email_processing_type(message):
    # 1. 除外リストチェック（最優先）
    if is_excluded(message):
        return EXCLUDE
    
    # 2. グループメールチェック
    if is_group_email(message):
        if has_my_name(message.body):
            return SUMMARY_ONLY
        return MARK_AS_READ
    
    # 3. To/CCチェック
    if my_email in message.to:
        return FULL_PROCESS
    if my_email in message.cc:
        return SUMMARY_ONLY
    
    # 4. BCC対応（名前チェック）
    if has_my_name(message.body):
        return SUMMARY_ONLY
    
    return MARK_AS_READ
```

### テンプレートシステム

変数置換を使った柔軟なテンプレート：

```python
template = "{sender_name}様\n\n{body}\n\n{my_name}"
result = template.format(
    sender_name="山田太郎",
    body="AI生成の本文",
    my_name="中村（康）"
)
```

## セキュリティ対策

- 環境変数で認証情報を管理
- `.env`ファイルは`.gitignore`に含める
- ローカル環境でのみ動作
- OpenAI APIへの送信内容に注意

## カスタマイズ例

### 除外リストの活用

```bash
# Teams通知は後で確認したいので未読維持
EXCLUDE_SENDER_DOMAINS=@teams.mail.microsoft

# セールスメールは不要なので既読
EXCLUDE_AND_READ_DOMAINS=newsletter@example.com
```

### テンプレートのカスタマイズ

もっとカジュアルにしたい場合：

```
{sender_name}さん

お疲れ様です！{my_name}です。

{body}

よろしくです！
```

## まとめ

このツールで得られるもの：

- ✅ 時間の節約（1日40分）
- ✅ メール見逃しゼロ
- ✅ 自然な返信文
- ✅ メール履歴の一元管理
- ✅ ストレス軽減

## 公開ファイル一覧

```
mail-automation/
├── README.md              # 詳細なドキュメント
├── SETUP_GUIDE.md         # セットアップガイド
├── INTRODUCTION.md        # システム紹介
├── env.example            # 環境変数テンプレート
├── requirements.txt       # 依存パッケージ
├── main.py               # メイン処理
├── config.py             # 設定管理
├── ai_processor.py       # AI処理
├── mail_processor.py     # メール処理（IMAP）
├── outlook_com_processor.py  # メール処理（Outlook COM）
├── ews_mail_processor.py     # メール処理（EWS）
├── graph_mail_processor.py   # メール処理（Graph API）
├── storage_manager.py    # Obsidian保存
├── utils.py              # ユーティリティ
├── setup.sh              # セットアップ（macOS）
├── run.sh                # 実行スクリプト
├── install_launchagent.sh # 自動実行設定（macOS）
└── templates/            # メールテンプレート
    ├── internal_template.txt
    └── external_template.txt
```

## おわりに

メール処理の自動化は、単なる時間の節約以上の価値があります。

- 重要なメールに集中できる
- 心理的な負担が減る
- 返信の質が向上する

ぜひ、あなたの環境に合わせてカスタマイズして使ってみてください！

**GitHubリポジトリ: （あなたのリポジトリURL）**

---

### タグ
#Python #メール自動化 #OpenAI #GPT4 #Outlook #Obsidian #業務効率化 #AI活用

### 関連記事
- Obsidianでナレッジベースを構築する方法
- OpenAI APIを業務で活用する方法
- Pythonで業務を自動化する

---

**この記事が役に立ったら、ぜひスキやフォローをお願いします！**

